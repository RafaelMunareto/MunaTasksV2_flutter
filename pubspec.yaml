name: munatasks2
description: MunaTasks versão 2.0.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 2.0.1+2

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  local_auth: ^1.1.8
  cupertino_icons: ^1.0.2
  flutter_modular: ^4.4.0
  mobx: ^2.0.5
  flutter_mobx: ^2.0.2
  build_runner: ^2.1.5
  path_provider:
  hive: ^2.0.5
  shared_preferences:
  google_fonts: ^2.1.1
  image_picker: ^0.8.5
  freezed_annotation: ^1.1.0
  layout: ^1.0.2
  http: ^0.13.4
  rolling_switch: ^0.0.2
  simple_animations: ^4.0.1
  supercharged: ^2.1.1
  accordion: ^2.1.2
  badges: ^2.0.2
  google_sign_in: ^5.3.0
  flutter_zoom_drawer: ^2.1.2
  flutter_iconpicker: ^3.1.4+1
  bubble_bottom_bar: ^2.0.0
  boardview: ^0.2.2
  intl: ^0.17.0
  date_format: ^2.0.5
  flutter_folding_card: ^1.0.0+1
  dio: ^4.0.6
  objectid: 2.0.0
  http_parser:
  flutter_sliding_up_panel: ^2.0.1
  dwds: ^13.1.0
  http_multi_server:
  file_picker: ^4.5.1
  flutter_session_manager: ^1.0.3
  uni_links: ^0.5.1
  crypto: ^3.0.2
  universal_html: ^2.0.8
  desktop_window: ^0.4.0
  flutter_local_notifications: ^9.5.2
  show_update_dialog: ^1.0.9
  package_info_plus: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  mobx_codegen: ^2.0.4
  freezed: ^1.1.0
  flutter_native_splash: ^1.3.2
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#ffffff"

flutter_native_splash:
  color: "#f7f6f4"
  image: "assets/icon/icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/exe/
    - assets/img/
    - assets/icon/icon.png

scripts:
  mobx: flutter pub run build_runner watch --delete-conflicting-outputs
  chrome: flutter run -d chrome --web-renderer html --web-port 52230
  build_web: flutter build web --web-renderer html && scp -r .\build\web\* root@45.82.75.3:/home/munatask/public_html/
  build_windows: flutter build windows
  build_bundle: flutter build appbundle --build-name=2.0.1+2 --build-number=21 --no-tree-shake-icons
  build_apk: flutter build apk --build-name=2.0.0+1 --build-number=2 --no-tree-shake-icons
  launch_icon: flutter pub run flutter_launcher_icons:main
  launch_splash: flutter pub run flutter_native_splash:create
