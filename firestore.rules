rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{usuarios=**} {
      allow read, write: if request.auth.uid != null;
    }
    match /{grupoEmail=**} {
      allow read, write;
    }
  }
}
